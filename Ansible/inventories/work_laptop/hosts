all:
  children:

    docker:
      hosts:
        work:
          ansible_host: work-ansible
          ansible_user: ansible

    opencode:
      hosts:
        work:
          ansible_host: work-ansible
          ansible_user: ansible